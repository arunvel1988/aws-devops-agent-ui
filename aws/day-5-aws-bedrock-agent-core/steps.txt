curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

sudo apt install -y nodejs

node --version
npm --version

sudo apt install -y curl unzip

curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"

unzip awscliv2.zip

sudo ./aws/install

aws sts get-caller-identity

npm install -g @aws/agentcore


agentcore --help

 docker buildx version


mkdir ~/agentcore-docker
cd ~/agentcore-docker

curl -LsSf https://astral.sh/uv/install.sh | sh

source ~/.bashrc

 aws bedrock list-foundation-models \
  --region us-east-1 \
  --query "modelSummaries[?contains(providerName, 'OpenAI')].[modelId,modelName]" \
  --output table
-------------------------------------------------------------
|                   ListFoundationModels                    |
+--------------------------------+--------------------------+
|  openai.gpt-5.6-terra          |  GPT-5.6 Terra           |
|  openai.gpt-oss-120b-1:0       |  gpt-oss-120b            |
|  openai.gpt-oss-20b-1:0        |  gpt-oss-20b             |
|  openai.gpt-oss-safeguard-120b |  GPT OSS Safeguard 120B  |
|  openai.gpt-5.6-luna           |  GPT-5.6 Luna            |
|  openai.gpt-5.6-sol            |  GPT-5.6 Sol             |
|  openai.gpt-oss-safeguard-20b  |  GPT OSS Safeguard 20B   |
+--------------------------------+--------------------------+


############################################



python3 -m venv .venv
source .venv/bin/activate

pip install -U bedrock-agentcore boto3
##############


agentcore create \
  --project-name DevOpsAgent \
  --name DevOpsAgent \
  --language Python \
  --framework Strands \
  --model-provider Bedrock \
  --memory none \
  --build CodeZip


#########

export AWS_REGION=us-east-1
export MODEL_ID="amazon.nova-2-lite-v1:0"


##########

agentcore dev

agentcore deploy

agentcore status

agentcore invoke --prompt "Hello, what can you do?"






