services:

  github-mcp:

    build: .

    container_name: github-remediation-mcp

    ports:
      - "8080:8080"

    environment:

      MCP_API_KEY: "${MCP_API_KEY}"

      GITHUB_TOKEN: "${GITHUB_TOKEN}"

    restart: unless-stopped
